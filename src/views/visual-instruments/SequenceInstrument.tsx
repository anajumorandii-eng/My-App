import React,{useState}from'react';import BoardShell from '../visual-boards/BoardShell';import{boardPair}from'../visual-boards/pair';import{STAGE_LABEL}from'../../lib/visualStudy';import{SEQUENCE_CONFIGS,type SequenceConfigId}from'../../lib/sequenceLab';import type{BoardProps}from'../visual-boards/types';import'./SequenceInstrument.css';
function Scene({id,value}:{id:SequenceConfigId;value:number}){const terms=id==='pa'?Array.from({length:5},(_,i)=>3+i*value):Array.from({length:5},(_,i)=>3*value**i);const y=(term:number,i:number)=>id==='pa'?160-i*value*9:220-Math.max(-5,Math.min(55,term))*3;return <>{terms.map((term,i)=><g key={i}><circle className="vs-seq-node" cx={45+i*58} cy={y(term,i)} r="15"/><text className="vs-seq-text" x={45+i*58} y={y(term,i)+5} textAnchor="middle">{term}</text>{i<4&&<path className="vs-seq-link" d={`M${60+i*58} ${y(term,i)}L${87+i*58} ${y(terms[i+1],i+1)}`}/>}</g>)}<text className="vs-seq-caption" x="160" y="270" textAnchor="middle">{id==='pa'?`somar ${value} em cada passo`:`multiplicar por ${value} em cada passo`}</text></>}
function short(t?:string){const s=t?.trim().split(/(?<=[.!?])\s/)[0]??'';return s.length>180?`${s.slice(0,176)}…`:s}
export function sequenceInstrument(id:SequenceConfigId){const c=SEQUENCE_CONFIGS[id];return function SequenceBoard(props:BoardProps){const pair=boardPair(props);const[v,setV]=useState(c.control.initial),r=c.readouts(v),pivot=r.find(x=>x.pivot)??r[0],first=props.map.nodes[0],second=props.map.nodes[2]??props.map.nodes.at(-1);return <BoardShell kicker="Laboratório de sequências" title={c.name} subtitle={c.question} condition={{label:id==='pa'?'Δ':'×',value:pivot.value}} ariaLabel={`Instrumento de sequências: ${props.map.title}`} emphasis={pair.emphasis} scene={<div className="vs-instrument vs-sequence-instrument"><svg className="vs-plane vs-sequence" viewBox="0 0 320 300" role="img" aria-label={`${c.name}; ${pivot.label}: ${pivot.value}`}><Scene id={id} value={v}/></svg><p className="vs-instrument-dica">mexa na regra e compare termos vizinhos</p><div className="vs-plane-controls"><div className="vs-plane-control"><label htmlFor={`seq-${id}`}><strong>{c.control.label}</strong><span>{c.control.description}</span><b>{v}</b></label><input id={`seq-${id}`} type="range" min={c.control.min} max={c.control.max} step={c.control.step} value={v} onChange={e=>setV(Number(e.target.value))}/></div></div><dl className="vs-plane-readouts">{r.map(x=><div key={x.label} data-pivot={x.pivot?'true':undefined}><dt>{x.label}</dt><dd>{x.value}</dd></div>)}</dl></div>} left={{label:STAGE_LABEL[first?.stage??'conceito'],headline:first?.label??props.map.title,detail:short(first?.excerpt),formula:c.formula}} right={{label:STAGE_LABEL[second?.stage??'aplicacao'],headline:second?.label??props.map.title,detail:short(second?.excerpt),formula:`${pivot.label} = ${pivot.value}`}} leftState={pair.leftState} rightState={pair.rightState} leftSelected={pair.leftSelected} rightSelected={pair.rightSelected} onSelectLeft={pair.selectLeft} onSelectRight={pair.selectRight} equation={{label:'Leitura da sequência',general:c.formula,condition:'mostra',reduced:pivot.value}} closing={c.insight}/>}}

import React,{useState}from'react';import BoardShell from'../visual-boards/BoardShell';import{boardPair}from'../visual-boards/pair';import{STAGE_LABEL}from'../../lib/visualStudy';import{VECTORS,type VectorId}from'../../lib/vectorsLab';import type{BoardProps}from'../visual-boards/types';
function short(t?:string){return(t?.split(/(?<=[.!?])\s/)[0]??'').slice(0,180)}function Scene({id,v}:{id:VectorId;v:number}){const x=id==='velocidade'?4:v,y=id==='vetores'?3:4;return <><path d="M45 235H280M70 260V45" stroke="var(--vs-ink)" strokeWidth="2"/><path d={`M70 235L${70+x*22} 235L${70+x*22} ${235-y*22}`} stroke="var(--vs-burgundy)" strokeWidth="3" fill="none"/><path d={`M70 235L${70+x*22} ${235-y*22}`} stroke="var(--vs-ink)" strokeWidth="4"/><text x="160" y="280" textAnchor="middle" style={{fontWeight:800,fill:'var(--vs-ink)'}}>{id==='composicao'?`rio = ${v} m/s`:'vetor resultante'}</text></>}
export function vectorsInstrument(id:VectorId){const c=VECTORS[id];return function VB(props:BoardProps){const p=boardPair(props);const[v,setV]=useState(c.control.initial),r=c.readouts(v),pivot=r[2],a=props.map.nodes[0],b=props.map.nodes[2]??props.map.nodes.at(-1);return <BoardShell kicker="Laboratório de vetores" title={c.name} subtitle={c.question} condition={{label:'→',value:pivot.value}} ariaLabel={`Instrumento de vetores: ${props.map.title}`} emphasis={p.emphasis} scene={<div className="vs-instrument"><svg className="vs-plane" viewBox="0 0 320 300" role="img" aria-label={`${c.name}; ${pivot.value}`}><Scene id={id} v={v}/></svg><div className="vs-plane-controls"><div className="vs-plane-control"><label htmlFor={`vec-${id}`}><strong>{c.control.label}</strong><span>{c.control.description}</span><b>{v}</b></label><input id={`vec-${id}`} type="range" min={c.control.min} max={c.control.max} step={c.control.step} value={v} onChange={e=>setV(Number(e.target.value))}/></div></div><dl className="vs-plane-readouts">{r.map(x=><div key={x.label} data-pivot={x.pivot?'true':undefined}><dt>{x.label}</dt><dd>{x.value}</dd></div>)}</dl></div>} left={{label:STAGE_LABEL[a?.stage??'conceito'],headline:a?.label??props.map.title,detail:short(a?.excerpt),formula:c.formula}} right={{label:STAGE_LABEL[b?.stage??'aplicacao'],headline:b?.label??props.map.title,detail:short(b?.excerpt),formula:pivot.value}} leftState={p.leftState} rightState={p.rightState} leftSelected={p.leftSelected} rightSelected={p.rightSelected} onSelectLeft={p.selectLeft} onSelectRight={p.selectRight} equation={{label:'Leitura vetorial',general:c.formula,condition:'mostra',reduced:pivot.value}} closing={c.insight}/>}}

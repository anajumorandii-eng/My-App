import React,{useState}from'react';import BoardShell from'../visual-boards/BoardShell';import{boardPair}from'../visual-boards/pair';import{STAGE_LABEL}from'../../lib/visualStudy';import{ENERGY,type EnergyId}from'../../lib/energyLab';import type{BoardProps}from'../visual-boards/types';
export function energyInstrument(id:EnergyId){const c=ENERGY[id];return function EB(p:BoardProps){const[v,setV]=useState(c.control.initial),r=c.readouts(v),pair=boardPair(p),a=p.map.nodes[0],b=p.map.nodes[2]??p.map.nodes.at(-1);return <BoardShell kicker="Laboratório de energia" title={c.name} subtitle={c.question} condition={{label:'E',value:r[0].value}} ariaLabel={`Instrumento de energia: ${p.map.title}`} emphasis={pair.emphasis} scene={<div className="vs-instrument"><svg className="vs-plane" viewBox="0 0 320 300" role="img" aria-label={`${c.name}; ${r[0].value}`}><rect x="55" y="170" width="210" height="40" fill="var(--vs-paper)" stroke="var(--vs-ink)"/><path d={`M70 145H${70+v*18}`} stroke="var(--vs-burgundy)" strokeWidth="8"/><text x="160" y="265" textAnchor="middle" style={{fontWeight:800,fill:'var(--vs-ink)'}}>{c.formula}</text></svg><div className="vs-plane-controls"><div className="vs-plane-control"><label htmlFor={`en-${id}`}><strong>{c.control.label}</strong><span>{c.control.description}</span><b>{v}</b></label><input id={`en-${id}`} type="range" min={c.control.min} max={c.control.max} step={c.control.step} value={v} onChange={e=>setV(Number(e.target.value))}/></div></div><dl className="vs-plane-readouts"><div data-pivot="true"><dt>{r[0].label}</dt><dd>{r[0].value}</dd></div></dl></div>} left={{label:STAGE_LABEL[a?.stage??'conceito'],headline:a?.label??p.map.title,detail:a?.excerpt??'',formula:c.formula}} right={{label:STAGE_LABEL[b?.stage??'aplicacao'],headline:b?.label??p.map.title,detail:b?.excerpt??'',formula:r[0].value}} leftState={pair.leftState} rightState={pair.rightState} leftSelected={pair.leftSelected} rightSelected={pair.rightSelected} onSelectLeft={pair.selectLeft} onSelectRight={pair.selectRight} equation={{label:'Leitura energética',general:c.formula,condition:'mostra',reduced:r[0].value}} closing={c.insight}/>}}

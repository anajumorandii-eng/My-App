import React,{useState}from'react';import BoardShell from'../visual-boards/BoardShell';import{boardPair}from'../visual-boards/pair';import{STAGE_LABEL}from'../../lib/visualStudy';import{KINEMATICS,type KinematicsId}from'../../lib/kinematicsLab';import type{BoardProps}from'../visual-boards/types';
function short(t?:string){const s=t?.trim().split(/(?<=[.!?])\s/)[0]??'';return s.length>180?`${s.slice(0,176)}…`:s}function Scene({id,v}:{id:KinematicsId;v:number}){const d=id==='mu'?`M52 ${185-v*8}L270 ${185+v*8}`:`M52 210 Q160 ${210-v*20} 270 ${210-v*5}`;return <><path d="M42 235H280M58 255V45" stroke="var(--vs-ink)" strokeWidth="2"/><path d={d} fill="none" stroke="var(--vs-burgundy)" strokeWidth="4"/><text x="164" y="275" textAnchor="middle" style={{fontWeight:800,fill:'var(--vs-ink)'}}>{id==='mu'?`v = ${v} m/s`:`a = ${v} m/s²`}</text></>}
export function kinematicsInstrument(id:KinematicsId){const c=KINEMATICS[id];return function KBoard(props:BoardProps){const p=boardPair(props);const[v,setV]=useState(c.control.initial),r=c.readouts(v),pivot=r[0],a=props.map.nodes[0],b=props.map.nodes[2]??props.map.nodes.at(-1);return <BoardShell kicker="Laboratório de cinemática" title={c.name} subtitle={c.question} condition={{label:'S×t',value:pivot.value}} ariaLabel={`Instrumento de cinemática: ${props.map.title}`} emphasis={p.emphasis} scene={<div className="vs-instrument"><svg className="vs-plane" viewBox="0 0 320 300" role="img" aria-label={`${c.name}; ${pivot.value}`}><Scene id={id} v={v}/></svg><p className="vs-instrument-dica">mexa no valor e leia a forma do gráfico</p><div className="vs-plane-controls"><div className="vs-plane-control"><label htmlFor={`kin-${id}`}><strong>{c.control.label}</strong><span>{c.control.description}</span><b>{v}</b></label><input id={`kin-${id}`} type="range" min={c.control.min} max={c.control.max} step={c.control.step} value={v} onChange={e=>setV(Number(e.target.value))}/></div></div><dl className="vs-plane-readouts">{r.map(x=><div key={x.label} data-pivot={x.pivot?'true':undefined}><dt>{x.label}</dt><dd>{x.value}</dd></div>)}</dl></div>} left={{label:STAGE_LABEL[a?.stage??'conceito'],headline:a?.label??props.map.title,detail:short(a?.excerpt),formula:c.formula}} right={{label:STAGE_LABEL[b?.stage??'aplicacao'],headline:b?.label??props.map.title,detail:short(b?.excerpt),formula:pivot.value}} leftState={p.leftState} rightState={p.rightState} leftSelected={p.leftSelected} rightSelected={p.rightSelected} onSelectLeft={p.selectLeft} onSelectRight={p.selectRight} equation={{label:'Leitura cinemática',general:c.formula,condition:'mostra',reduced:pivot.value}} closing={c.insight}/>}}
